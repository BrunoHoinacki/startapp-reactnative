import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        width: '80%',
        height: 60,
        borderRadius: 10,
        borderWidth: 1,
        borderColor: '#0654ba',
        color: '#0654ba',
        padding: 16,
        fontSize: 16,
        marginRight: 8
    }
});